import React, { Component, Fragment } from 'react';
import Headline from './Components/Headline';

class Mapper extends Component {
  render() {
    return (
      <Fragment>
        <Headline />
        <h1>This is mapper</h1>
      </Fragment>
    );
  }
}

export default Mapper;
